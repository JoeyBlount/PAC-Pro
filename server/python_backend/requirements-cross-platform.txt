# Cross-platform requirements for PAC Backend
# Core FastAPI dependencies (works on all platforms)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
httpx>=0.25.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Platform-specific Firebase dependencies
# Note: Firebase may have compilation issues on some platforms
# The app will fall back to mock data if Firebase is not available
firebase-admin>=6.4.0
google-cloud-firestore>=2.13.0

# Optional: Image processing (may require compilation on ARM64)
# pillow>=10.1.0  # Uncomment if needed for image processing
